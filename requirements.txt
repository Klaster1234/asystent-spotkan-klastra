# Python 3.10+
# Interfejs webowy (WebView2) + lokalne nagrywanie z mikrofonu.
# Na Windows pywebview pociaga za soba pythonnet automatycznie i korzysta z
# Microsoft Edge WebView2 Runtime (instalator dograje go w razie potrzeby).
pywebview>=5.0
sounddevice>=0.4
